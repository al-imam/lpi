import Links from "@components/Links/Links";
import data from "./data";

function RecentNotice() {
  return <Links text="More Notice" data={data} page={true} />;
}

export default RecentNotice;
